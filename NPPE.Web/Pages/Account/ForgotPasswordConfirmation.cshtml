@page
@model NPPE.Web.Pages.Account.ForgotPasswordConfirmationModel
@{
    ViewData["Title"] = "Check your email";
    Layout = "_Layout";
}

<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full">
        <!-- Confirmation Card -->
        <div class="bg-white rounded-2xl shadow-xl p-8 text-center">
            <!-- Success Icon -->
            <div class="flex justify-center mb-6">
                <div class="w-20 h-20 bg-gradient-to-r from-green-100 to-emerald-100 rounded-full flex items-center justify-center">
                    <svg class="h-10 w-10 text-green-600" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                        <polyline points="22,6 12,13 2,6"></polyline>
                    </svg>
                </div>
            </div>

            <!-- Title -->
            <h1 class="text-3xl font-bold text-gray-900 mb-3">
                Check Your Email
            </h1>

            <!-- Message -->
            <p class="text-gray-500 mb-6">
                @TempData["StatusMessage"]
            </p>

            <!-- Additional Info -->
            <div class="bg-blue-50 rounded-xl p-4 mb-6">
                <p class="text-sm text-blue-700">
                    Didn't receive the email? Check your spam folder or try again.
                </p>
            </div>

            <!-- Back to Login Link -->
            <a asp-page="/Account/Login"
               class="inline-flex items-center justify-center w-full py-3 px-4 bg-gradient-to-r from-blue-600 to-indigo-600 text-white font-semibold rounded-xl shadow-lg hover:from-blue-700 hover:to-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-200 transform hover:scale-[1.02]">
                Back to Sign in
            </a>

            <!-- Try Again Link -->
            <div class="mt-6">
                <a asp-page="/Account/ForgotPassword" class="text-gray-500 hover:text-blue-600 text-sm transition duration-200">
                    Try with a different email
                </a>
            </div>
        </div>
    </div>
</div>